// Closures

// Lexiccal-scope
/**
 * a varible that us used by a function defined outside the scope of it will be acessible inside it if there is no stack of outer function also
 * but its oppopsite is not true
 */
var name ='Mukul Anand Bunakar'

//global scope
function local () {
    //local scope
}
